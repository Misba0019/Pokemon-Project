# Setting the default behavior to automatically normalize line endings
* text=auto

# Explicitly declaring text files I want to always normalize
*.html text
*.css text
*.js text

# Ensuring all text files use LF line endings
* text eol=lf